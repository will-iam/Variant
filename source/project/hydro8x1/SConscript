Import('localenv')
localenv.Append(CXXFLAGS=['-DQUANTITY_SoA'])

localenv = localenv.Clone()

#Specialize environment

objs = []
# build sources
srcs = Glob('*.cpp')
o = localenv.Object(srcs)
objs.append(o)

Return('objs')
